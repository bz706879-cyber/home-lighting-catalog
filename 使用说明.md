# 使用与部署说明

## 前台

打开 `index.html` 查看商品展示。正式部署到 GitHub Pages 后，通过公开网址访问。

## 后台

打开 `admin.html`。支持商品搜索、新增、编辑、上架、下架、删除和导入/导出数据。

GitHub Pages 没有数据库，后台操作只保存在当前页面内存中。修改完成后必须点击“导出更新数据”，获得新的 `data.json`，再用它覆盖仓库根目录中的同名文件并提交，前台才会永久更新。

新增图片时，先把图片加入网站资源，再在商品编辑框中填写对应相对路径。对于不熟悉代码的维护人员，建议由技术人员统一上传图片和更新数据。

## GitHub Pages

仓库 Settings → Pages → Source 选择 Deploy from a branch，分支选择 main，目录选择 /(root)，保存即可。
